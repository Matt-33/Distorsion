<!-- chat.phtml -->
<div class="chat-box" id="chatBox">
    <?php if (isset($nomDuSalon) && isset($messages)): ?>
        <h2><?= htmlspecialchars($nomDuSalon, ENT_QUOTES, 'UTF-8') ?></h2>

        <div class="message-container">
            <!-- Affichez les messages existants -->
            <?php foreach ($messages as $message): ?>
                <div class="message"><?= htmlspecialchars($message['contenu'], ENT_QUOTES, 'UTF-8') ?></div>
            <?php endforeach; ?>
        </div>

        <!-- Formulaire pour envoyer un message -->
        <form method="post" action="index.php?action=sendMessage">
            <input type="hidden" name="SalonId" value="<?= htmlspecialchars($salonId, ENT_QUOTES, 'UTF-8') ?>">
            <label for="MessageContent">Message :</label>
            <input type="text" id="MessageContent" name="MessageContent" required>
            <button type="submit">Envoyer</button>
        </form>
    <?php else: ?>
        Aucun message trouvé pour ce salon.
    <?php endif; ?>
</div>