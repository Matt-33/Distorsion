<!-- chat.phtml -->
<div class="chat-box" id="chatBox">
    <?php if (isset($nomDuSalon) && isset($messages)): ?>
        <h2><?= $nomDuSalon ?></h2>

        <div class="message-container">
            <!-- Affichez les messages existants -->
            <?php foreach ($messages as $message): ?>
                <div class="message"><?= $message['contenu'] ?></div>
            <?php endforeach; ?>
        </div>

        <!-- Formulaire pour envoyer un message -->
       <form method="post" action="index.php?action=sendMessage">
    <input type="hidden" name="SalonId" value="<?= $salonId ?>">
    <label for="MessageContent">Message :</label>
    <input type="text" id="MessageContent" name="MessageContent" required>
    <button type="submit">Envoyer</button>
</form>
    <?php else: ?>
        Aucun message trouvé pour ce salon.
    <?php endif; ?>
</div>