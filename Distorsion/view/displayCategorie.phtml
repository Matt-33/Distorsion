<div class="categories-and-salons">
    <div class="categories">
        <h2>Catégories</h2>
        
        <!-- Bouton pour afficher le formulaire de création de catégorie -->
        <form method="post" action="index.php?action=showCreateCategoryForm">
            <button class="add-button">+ Ajouter une catégorie</button>
        </form>

        <!-- Affichage des catégories existantes -->
        <ul id="categoryList">
            <?php if (!empty($categories)): ?>
                <?php foreach ($categories as $category): ?>
                    <li>
                        <?= $category['name'] ?>
                        <form method="post" action="index.php?action=createSalon">
                            <input type="hidden" name="CategorieId" value="<?= $category['Id'] ?>">
                            <button class="add-button">+ Ajouter un salon</button>
                        </form>

                        <!-- Affichage des salons pour cette catégorie -->
                        <ul>
                            <?php if (!empty($category['Salon'])): ?>
                                <?php foreach ($category['Salon'] as $salon): ?>
                                    <li>
                                        <!-- Lien pour charger dynamiquement le chat via PHP -->
                                        <a href="index.php?action=displaySalon&SalonId=<?= $salon['Id'] ?>">
                                            <?= $salon['name'] ?>
                                        </a>
                                    </li>
                                <?php endforeach; ?>
                            <?php else: ?>
                                <li>Aucun salon trouvé pour cette catégorie.</li>
                            <?php endif; ?>
                        </ul>
                    </li>
                <?php endforeach; ?>
            <?php else: ?>
                <li>Aucune catégorie trouvée.</li>
            <?php endif; ?>
        </ul>
    </div>

    <!-- Affichage du chat -->
    <?php include 'view/chat.phtml'; ?>
</div>